```declarative
2. Using Stream.reduce()
    * Accumulator function is the lambda expression 
    * Accumulator function is the exiting JAVA method 
    * Accumulator function is our custom method.
3. Using Stream.collect()
    * Using Integer::intValue in the collector mapper
    * Using Integer::ValueOf in the collector mapper
4. Using IntStream.sum()
    * Using Integer::intValue in the mapToInt
    * Using Integer::vallueOf in the mapToInt
5. Using Stream#sum With Map
    * Using Integer::intValue in the mapToInt
    * Using Integer::vallueOf in the mapToInt
6. Using Stream#sum With Objects
    * Stream.reduce()
    * Stream.collect()
    * IntStream.sum() 
7. Using Stream#sum With String
```